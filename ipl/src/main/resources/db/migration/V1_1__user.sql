CREATE TABLE IF NOT EXISTS "user" (
                    id varchar(100),
                    emailId varchar(100),
                    userName varchar(100),
                    password varchar(100),
                    primary key(id)
                    )